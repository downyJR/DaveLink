import { TypedEventEmitter } from '../core/EventEmitter';
import type { Node } from '../node/Node';
import type { Track, PlayOptions, Filters, EqualizerBand, VoiceUpdateOptions, PlayerOptions, PlayerState, VoiceState } from '../types';
export declare class Player extends TypedEventEmitter {
    guildId: string;
    node: Node;
    state: PlayerState;
    voiceState: VoiceState;
    destroyed: boolean;
    lyricsData: unknown;
    sponsorBlockSegments: unknown[];
    private _lyricsEnabled;
    private _sponsorBlockEnabled;
    private _daveEnabled;
    maxQueueSize: number;
    circularQueue: boolean;
    private _managerEmit;
    private _boundListeners;
    private _trackListenerCount;
    constructor(guildId: string, node: Node, options?: PlayerOptions, managerEmit?: (event: string, ...args: unknown[]) => void);
    get lavaSessionId(): string | null;
    play(options?: PlayOptions): Promise<void>;
    pause(): Promise<void>;
    resume(): Promise<void>;
    stop(): Promise<void>;
    skip(): Promise<void>;
    seek(position: number): Promise<void>;
    setVolume(volume: number): Promise<void>;
    queueAdd(track: Track | string, position?: 'front' | 'back'): void;
    queueRemove(index: number): Track | undefined;
    queueClear(): void;
    queueGet(): Track[];
    queueShuffle(): void;
    join(channelId: string): Promise<void>;
    leave(): Promise<void>;
    voiceUpdate(options: VoiceUpdateOptions): Promise<void>;
    setFilters(filters: Filters): Promise<void>;
    setEqualizer(bands: EqualizerBand[]): Promise<void>;
    clearFilters(): Promise<void>;
    enableSponsorBlock(): void;
    disableSponsorBlock(): void;
    setSponsorBlockCategories(categories: string[]): Promise<void>;
    getSponsorBlockSegments(): unknown[];
    enableLyrics(): void;
    disableLyrics(): void;
    getLyrics(): unknown;
    fetchLyrics(skipTrackSource?: boolean): Promise<unknown>;
    enableDaveE2EE(): void;
    disableDaveE2EE(): void;
    isDaveEnabled(): boolean;
    get currentTrack(): Track | null;
    get previousTrack(): Track | null;
    get position(): number;
    get paused(): boolean;
    get volume(): number;
    get channelId(): string | null;
    get queueLength(): number;
    get isPlaying(): boolean;
    get isPaused(): boolean;
    get isConnected(): boolean;
    get stateSnapshot(): PlayerState & {
        voice: VoiceState;
    };
    get filters(): Filters;
    get lyricsEnabled(): boolean;
    get sponsorBlockEnabled(): boolean;
    destroy(): Promise<void>;
    migrateTo(newNode: Node): Promise<void>;
    updateFromServer(state: {
        volume?: number;
        position?: number;
        paused?: boolean;
    }): void;
    toJSON(): Record<string, unknown>;
    fromJSON(data: Record<string, unknown>): void;
    private _ensureNotDestroyed;
    private _updatePlayer;
    private _setupEventForwarding;
    private _cleanupEventForwarding;
}
export declare class PlayerManager {
    private players;
    private nodes;
    destroyed: boolean;
    registerNode(node: Node): void;
    unregisterNode(nodeId: string): void;
    getPlayer(guildId: string): Player | undefined;
    createPlayer(guildId: string, node: Node | undefined, options: PlayerOptions, managerEmit?: (event: string, ...args: unknown[]) => void): Player;
    destroyPlayer(guildId: string): Promise<void>;
    getPlayers(): Player[];
    getPlayerCount(): number;
    destroyAll(): Promise<void>;
    private _selectBestNode;
}
//# sourceMappingURL=Player.d.ts.map